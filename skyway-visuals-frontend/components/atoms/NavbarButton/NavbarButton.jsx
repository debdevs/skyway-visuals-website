import React from 'react';
import styles from './NavbarButton.module.css';
const NavbarButton = () => {
  return <button className={styles.navbar_button}>Home</button>;
};

export default NavbarButton;
